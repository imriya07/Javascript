for (var r = 1; r <= 4; r++){
    let txt = "";
    for(var b = 1; b <= 4-r;b++){
        txt = txt + ""
    }
    for(var s=1; s <= 2 * (r-1) - 1;s++){
        txt = txt + "*"
    }
    console.log(txt)
    console.log("\n")
}

